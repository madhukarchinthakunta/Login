import styled from "styled-components"
import { Sidebar } from "react-pro-sidebar";
export const Paycaps=styled('div')(()=>({
    // height: "100vh",
    display:'flex'
}))

export const SidebarUpper=styled(Sidebar)(()=>({
    height: "105vh",
   backgroundColor:'black'
}))

